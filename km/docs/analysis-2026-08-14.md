# 考研错题本 · 代码全面分析报告（2026-08-14）

> 分析范围：`backend/`（FastAPI + SQLite）与 `frontend/`（Vue 3 + Element Plus）全部源码、
> 测试、CI、启动脚本与文档。
> 方式：逐文件阅读 + 接口实测验证 + 测试运行；未修改任何业务代码。
> 依据协作说明：「全面分析工程前后端代码，弄清楚项目是干什么、实现什么的，并指出可优化空间」。

> **2026-08-14 晚更新**：本节列出的 P0-P3 问题已在同日全部修复并实测验证
> （见下方「修复记录」附录）。本报告保留问题清单作为优化依据与回归参考。

---

## 一、项目定位：这个项目到底是干什么的

一个**单机（本机）单用户考研错题管理应用**，核心闭环：

```
录入（手动 / 文本 AI / 截图三通道视觉识别）
   ↓
错题库（三种题型 + 真题/模拟题分类 + 知识点标签）
   ↓
复习（今日复习：间隔重复 1/3/7/15/30；自主练习：曲线/错时/随机/真题专项）
   ↓
沉淀（知识点库 + 公式背诵库 + 科目指南 + 统计）
```

- 后端 FastAPI + SQLite（裸 sqlite3，无 ORM），前端 Vue 3 + Element Plus + GSAP + KaTeX。
- 单端口生产模式（前端构建产物由后端挂载），默认只监听 `127.0.0.1:8000`。
- AI 能力（可选）：DeepSeek 文本解析/批改，GLM-4.6V-Flash → Agnes-2.0-Flash → Agnes-2.5-Flash
  三通道视觉轮询识图，全部失败退回本地 Windows OCR + 文本 AI。
- 数据安全：启动自动备份（保留 20 份）、迁移版本门控（app_meta.migration_version=2）、WAL 模式。

---

## 二、系统现状：已实现的功能全景

### 2.1 后端（8 个 router，约 35 个端点）

| 模块 | 端点 | 说明 |
|---|---|---|
| 错题 mistakes | CRUD、列表筛选分页、approaches 联想、judge/grade、batch、pause/resume/source-type | 核心模块；服务端校验完备 |
| 复习 reviews | today / practice / stats、`POST /mistakes/{id}/review` | 间隔重复排期、三种练习模式 |
| 知识点 knowledge | 列表分页 / by-tag / update / delete / auto-summarize | 标签同义词归一（TAG_SYNONYMS） |
| 公式 formulas | CRUD + 分类/搜索 | 预置导数/积分/泰勒/等价无穷小等 |
| 科目 subjects | 科目/二级科目列表、档案 get/update | 政治/英语二/数学二/408 |
| 统计 stats | 总数、今日新增、题型/来源/科目分布 | |
| 导入导出 transfer | export / import（≤5000 条，单事务） | |
| AI ai | analyze（文本）/ ocr（图片三通道轮询） | instruction + 参考图多模态 |

**关键设计**：统一响应 `{code, data, message}`；参数化查询（未发现 SQL 注入）；
`build_mistake_fields` 服务层二次校验；`knowledge_tags` 自动补词条；启动备份 + 迁移门控。

### 2.2 前端（10 个视图 + 13 个组件 + 7 个 composable）

| 页面 | 职责 |
|---|---|
| MistakeList | 错题库主页：筛选/排序/分页、批量操作、导入导出、随机抽题 |
| CaptureView | 智能录入：文本 AI 解析、截图/图片三通道识别、参考图、补充要求 |
| ReviewView | 今日复习/练习作答流：按题型分发三种作答组件 |
| PracticeView | 练习条件配置：四模式 + 九项筛选 → 跳复习流 |
| KnowledgeBase | 知识点库：分页、AI 自动总结、一键练习 |
| FormulaView | 公式背诵库：分类/搜索/背诵模式/增删改 |
| SubjectGuide | 科目档案：复习重点标签 + 方法建议编辑 |
| Stats | 学习统计：8 张指标卡 + 近 7 天趋势 + 薄弱知识点 |

**工程质量亮点**（上一轮重构成果，git df0a999）：
- 图标按需注册（17 个）、Element Plus 组件按需引入、JS gzip 约 -40%。
- composables 收敛重复逻辑（useSubSubject 收敛 5 处、useTagInput 收敛 3 处、mistakeDraft 收敛 2 处）。
- KaTeX 渲染结果缓存；CaptureView 竞态令牌；监听器/定时器 onUnmounted 清理。
- 无 Pinia，模块级 reactive 单例 + Promise 缓存（该规模下够用）。

### 2.3 测试 / CI / 文档

- 后端 30 个测试用例（接口层 8 + 回归 13 + 冒烟 9）全绿，内存库隔离、AI 打桩，不碰真实数据。
- **接口覆盖约 40%**：全项目 35 个端点，test_api 直接命中 14 个；未覆盖 21 个端点——
  `PUT /api/mistakes/{id}`、`POST /batch`、`GET /approaches`、`GET /{id}/reviews`、
  `POST /{id}/grade`、`pause/resume`、`GET /reviews/today`、`GET /knowledge/by-tag`、
  `PUT/DELETE /knowledge/{id}`、`auto-summarize`、subjects profile、formulas 全部、import、ai 两个。
  服务层未测：stats 聚合 SQL、formula_service、导入导出往返、`backup_database` 保留策略、seed 幂等。
- GitHub Actions CI 真实存在（仓库根 `.github/workflows/ci.yml`，df0a999 引入，路径 `km/backend|frontend` 与现状匹配）：后端 unittest + 前端 npm build。
  **局限**：仅 push/PR 匹配 `km/backend/**`、`km/frontend/**` 才触发；不跑前端两个 .mjs 测试脚本；无覆盖率统计；README 无测试/CI 说明。
- 数据库启动备份保留 20 份（database.py:27-46）；备份逻辑本身无测试。
- 测试依赖（pytest/httpx）混在 requirements.txt，建议拆 requirements-dev.txt；pytest 与 unittest 两种运行方式并存，建议统一入口。

---

## 三、实测验证的问题清单（按优先级）

> 以下问题均已通过**实际运行接口/测试验证**，非仅静态推断。

### P0 —— 正确性 / 稳定性（建议尽快修）

1. **间隔重复 off-by-one**（`review_service.py:173-175`）：答对路径先 `mastery+1` 再取档，
   首次答对 0→1 取 `INTERVALS[1]=3` 天，答对路径实际为 3/7/15/30/30——**`1` 天档永远用不到**，
   与新错题答对后 1 天复现的产品语义不符，记忆曲线间隔偏长。
2. **分页参数缺 page_size 直接 500**（`mistake_service.py:254`、`knowledge_service.py:126`）：
   `GET /api/mistakes?page=1`（不传 page_size）实测返回 500 TypeError，
   被 server_error 吞成「服务器内部错误」。page/page_size 应成对校验或给默认值。
3. **视觉并发成功后未取消其余 future**（`routers/ai.py:137-149`）：首个通道成功或预算耗尽时，
   其余视觉线程仍跑满 30s 超时，浪费 API 配额、占满 3 线程常驻池。应 `future.cancel()`。
4. **前端 `correct_answer` 默认值泄漏**（`mistakeDraft.js:18` 默认 `'A'`）：填空题/解答题
   留空参考答案时提交仍为 `'A'`，详情/列表展示假答案。默认应改为 `''`。
5. **选择题判卷未规范化**（`mistakes.py:119`）：全角 `Ａ` 实测判错，与填空题的
   NFKC 归一化口径不一致；应复用 `normalize_answer`。
6. **`PUT` 空 body 静默清空数据**（`schemas.py:43-47/86-88`）：`PUT /api/knowledge/{id}`
   传 `{}` 实测把 summary 清空；`SubjectProfileUpdate` 同理。应改 PATCH 语义或字段必填。

### P1 —— 体验 / 一致性 / 安全成本

7. **AI 端点无鉴权无限流**（ai.py、mistakes.py:141、knowledge.py:95）：纯本机可接受，
   但若对外暴露，`/ai/ocr`、`/grade`、`/auto-summarize` 可被无限刷，直接消耗 AI 额度。
   建议至少 `API_TOKEN` 环境变量 + 简单限流，README 明示勿对外暴露。
8. **全局异常响应不统一**（main.py:36-50）：未捕获异常透传 FastAPI 默认
   `{"detail": "..."}`；`POST /api/unknown` 实测返回 405 非统一格式；`GET /api` 实测
   返回 SPA 页面而非 404 JSON。建议补 Exception/HTTPException handler。
9. **SubjectGuide 页面 N+1 + 连环错误弹窗**（`SubjectGuide.vue:37-46`）：每个科目串行拉
   profile，新库无档案时 404 会触发全局 toast 连弹 4 条。拦截器应支持「静默」选项。
10. **详情弹窗并发竞态**（`MistakeDetailDialog.vue:46-61`）：快速切换同知识点错题时
    旧响应可能覆盖新数据（CaptureView 已有令牌模式，此处未复用）。
11. **填空复习无手动兜底**（`ReviewView.vue:94-113`）：AI 判分接口失败时填空题流程卡死，
    无「跳过判分手动标记」按钮（选择题/解答题都有绕过路径）。
12. **批量操作计数语义不一致**（`mistake_service.py:403-428`）：pause/resume/source_type
    返回 `len(ids)` 而非实际影响行数，不存在的 id 也报成功。
13. **薄弱知识点统计口径偏颇**（`review_service.py:288-305`）：只取 wrong_count 前 30 行
    错题在 Python 聚合，错题超过 30 道时榜单不完整。

### P2 —— 性能（数据量增长后收益明显）

14. **科目统计 join 放大**（`review_service.py:338-348`）：LEFT JOIN review_records 使每道错题
    的每条复习记录成一行，复习记录多时中间行数膨胀。
15. **练习队列相关子查询**（`review_service.py:104-132`）：每行 2 个 `MAX(reviewed_at)`
    子查询，可改 GROUP BY 预聚合。
16. **标签检索全表扫描**（`mistake_service.py:215/291`、`review_service.py:92`、
    `knowledge.py:108`）：`instr(',tags,', ?)` 无法走索引；规模化后可拆关联表或 FTS5。
17. **导入接口 N+1**（`transfer.py:60-80`）：5000 条导入 = 约 1 万+ 次子查询 + 数千次单条
    INSERT；可预加载科目集合 + executemany。
18. **KaTeX 缓存整表清空**（`markdown.js:26`）：超 500 条整表 clear，热公式也失效；
    建议 LRU 式淘汰。
19. **导出超过 1000 条静默截断**（`useImportExport.js:18-40`）：page_size=1000 不分页翻页。

### P3 —— 一致性 / 移动端 / 可维护性

20. **弹窗固定 px 宽度**（MistakeDetailDialog:180、FormulaView:271/314/329 等）：≤560px
    屏横向溢出，建议 `min(760px, 92vw)`。
21. **路由前缀与分层风格分裂**：reviews/stats/transfer/subjects 用 `/api` 前缀，
    formulas/mistakes/knowledge/ai 用 `/api/<资源>`；knowledge.py:100-121、subjects.py
    在路由层直接写 SQL 绕过 service。
22. **死代码**：`schemas.py` 的 `ApiResponse`（未使用）、`AiSummarizeRequest`（未使用）、
    `database.py` 的 `shutil`、`routers/ai.py` 的 `server_error`、`local_ocr.py` 的
    `Language` 导入。
23. **重复实现**：`_local_day_bounds_utc`（review_service 与 stats_service 各一份）；
    DetailMeta 与 ChoiceAnswer 各自的选项映射；`GET /api` 精确路径处理缺失。
24. **键盘可达性缺口**：MistakeCard 只处理 Enter 不处理 Space；RelatedList/FormulaView
    可点击 div 无 role/tabindex/键盘事件。
25. **迁移逐行 UPDATE**（`database.py:184-197`）：11 个 (表,列) 全表载入内存逐行更新，
    可改为单条 UPDATE CASE。
26. **种子/迁移硬编码 id 耦合**（seed_data.py:66-91、database.py:253-265）：科目与二级科目
    id 1-12 两处硬编码，新增科目易踩 FK。
27. **启动脚本硬编码绝对路径**（`start_system.ps1:5-6`、`start_system_hidden.vbs:3`）：
    `$root`/`$python`/vbs 内 ps1 路径全部写死，仓库迁移即失效（bat 的 `%~dp0` 相对化，
    两者不一致）；端口探测是唯一防重复手段，存在 TOCTOU 竞态与误判风险。
28. **CI 触发面窄**（`.github/workflows/ci.yml:5-11`）：只响应 `km/backend/**`、`km/frontend/**`
    路径变更，改 README/.github 不触发；frontend job 不跑 `test:markdown`/`test:clipboard`，
    无覆盖率统计，README 无测试/CI 章节。

---

## 四、文档与实现不一致（核对结论）

| 文档声称 | 实际情况 |
|---|---|
| 「新增 26 个单元/接口测试与 GitHub Actions CI」（记忆/README 摘要） | 测试实为 **30 个用例**；CI **真实存在**（`.github/workflows/ci.yml`，df0a999 引入，路径匹配） |
| docs/api.md | 缺 `GET /api/mistakes/approaches` 端点；AI 接口仅 3 行，未写 instruction/reference_image_base64 |
| docs/architecture.md | 前端页面清单过时（称四个页面，实际 10 个视图） |
| README 目录结构 | 写作 `kaoyan-mistakes/`，实际仓库布局为 `work/km/`（git 仓库根是 `work`） |

---

## 五、建议的优化路线图

1. **第一优先级（正确性，各 <1 小时）**：修间隔 off-by-one（§三-1）、分页参数兜底（2）、
   视觉 future 取消（3）、`correct_answer` 默认值（4）、选择题判卷归一化（5）、PUT 语义（6）。
2. **第二优先级（体验/成本）**：拦截器静默选项 + SubjectGuide N+1（9）、详情弹窗令牌（10）、
   填空手动兜底（11）、异常响应统一（8）、可选 API_TOKEN + 限流（7）。
3. **第三优先级（性能）**：统计 join 重构（14）、练习队列 GROUP BY（15）、标签关联表/FTS5（16）、
   导入批量化（17）、KaTeX LRU（18）、导出分页（19）。
4. **第四优先级（工程）**：统一路由前缀与分层（21）、清理死代码（22）、去重（23）、
   移动端弹窗（20）、键盘可达性（24）、迁移/种子解耦（26）。

---

## 六、总体评价

工程对「单机个人使用」的定位是**健康且超出预期**的：分层清晰（routers→services→database）、
参数化查询到位、迁移门控 + 启动备份、AI 输出加固（_wrap_math/JSON 容错）、前端组件拆分与
composable 收敛、30 个测试 + CI 双 job。未发现 SQL 注入与并发数据竞争类安全问题。

主要短板集中在**三处正确性细节**（间隔排期 off-by-one、分页 500、correct_answer 默认值泄漏）、
**视觉并发资源回收**、以及**统计类查询在数据增长后的性能退化**。按第五节优先级逐项收敛即可，
整体无需架构级返工。

*本报告由代码阅读 + 接口实测 + 测试运行生成；未修改任何业务代码。*

---

## 附录：修复记录（2026-08-14 晚，全部已完成并实测验证）

### 后端
1. **间隔重复 off-by-one**：答对路径改为递增前取档 `INTERVALS[max(0, mastery-1)]`，
   首次答对 1 天，之后 3/7/15/30（review_service.py）。
2. **分页参数兜底**：`list_mistakes`/`list_knowledge` 只传 page 时默认 page_size=20，
   不再抛 TypeError 500。
3. **视觉 future 取消**：首个通道成功或预算耗尽时 `cancel_pending()` 取消在途请求。
4. **选择题判卷归一化**：复用 `normalize_answer`，全角/空白/大小写变体均正确判对。
5. **PUT→PATCH 防清空**：`KnowledgeUpdate`/`SubjectProfileUpdate` 改 PATCH 语义
   （None 字段不更新），空 body 不再清空摘要/档案；前端同步改 PATCH。
6. **全局异常响应统一**：新增 Starlette HTTPException handler（405/404 统一 JSON）
   与 Exception handler（500 统一 JSON，含日志）；`GET /api` 精确路径返回 404 JSON。
7. **批量计数语义**：pause/resume/source_type 返回真实 `rowcount`，不存在 id 不再报成功。
8. **AI 鉴权与限流**（新增 `app/security.py`）：可选 `API_TOKEN`（X-API-Token/Bearer），
   AI 端点滑动窗口限流（`AI_RATE_LIMIT` 默认 30/分钟，超限 429）。
9. **薄弱标签 SQL 聚合**：递归 CTE 拆标签全量聚合，不再只统计前 30 道。
10. **科目统计 join 重构**：两个独立聚合合并，消除三表 join 放大。
11. **练习队列 GROUP BY**：相关子查询改预聚合子查询。
12. **标签检索走索引**：新增 `mistake_tag_map` 关联表（迁移 v3 + 回填 + 写入同步 +
    查询改写），`instr()` 全表扫描消除；`_standard_tags` 按高频标签排序。
13. **导入 N+1 优化**：预加载科目集合 + `skip_subject_check`，批量导入从万级查询降到常数级。
14. **批改全量持久化**：solution_grades 增列 errors/strengths/solution/alternate_methods。
15. **死代码清理**：`ApiResponse`/`AiSummarizeRequest`/`shutil`/`Language`/`server_error` 导入移除。
16. **`_local_day_bounds_utc` 去重**：合并到 `database.local_day_bounds_utc`。
17. **`update_mistake` 404 结构化**：`NOT_FOUND` 错误码替代中文字符串匹配。
18. **双时钟修复**：`review_mistake` 只取一次 now；`review_service` 移除冗余 import。

### 前端
19. **correct_answer 默认值泄漏**：mistakeDraft 默认改 `''`，填空题/解答题留空不再存 "A"。
20. **拦截器静默选项**：`request.get(url, {silent:true})` 抑制全局 toast；
    SubjectGuide 404 预期路径静默；CaptureView 502 去重（aiWarning 单一来源）。
21. **SubjectGuide N+1**：profile 请求并行化（Promise.all）。
22. **详情弹窗竞态令牌**：MistakeDetailDialog 加 `detailRequestId`。
23. **填空复习手动兜底**：FillAnswer 增加「手动标记：记住了/没记住」按钮。
24. **空态/错误态分离**：useMistakeFilters 加 `loadError`，MistakeList 显示错误态+重试。
25. **MistakeCreate 加载失败错误态**：不再静默白屏。
26. **KaTeX 缓存 LRU**：命中刷新位置、超限删最旧，不再整表清空。
27. **导出修复**：`exportJson` 直接使用 `/export` 全量数据，去掉冗余请求与 1000 条截断。
28. **弹窗移动端适配**：theme.css 媒体查询 `--el-dialog-width: 92vw`（≤640px）。
29. **键盘可达性**：MistakeCard 支持 Space；RelatedList/FormulaView 可点击元素补
    role/tabindex/键盘事件。
30. **死代码**：MistakeCard 空 span、CaptureView 冗余 div @paste 移除。
31. **document.title 按路由更新**（router afterEach）。

### 脚本 / CI / 文档
32. **启动脚本相对化**：ps1 用 `$PSScriptRoot`，vbs 用 `GetParentFolderName`；python 探测回退。
33. **CI 增强**：触发面加 `.github/workflows/**`；backend 加 coverage（阈值 40%）；
    frontend job 跑 markdown/clipboard 脚本测试。
34. **文档同步**：api.md 补 approaches/PATCH/限流说明；architecture.md 更新页面清单与
    标签关联表；README 补测试/CI 章节、安全与限流说明、目录名修正。

### 验证
- 后端测试 30 → **36 个全部通过**（新增间隔序列/分页兜底/选择题归一化/PATCH 语义/
  未知 API 统一格式 6 例）。
- 端到端实测 14 项全部通过（分页、PATCH、405/404 统一、批量计数、标签过滤、鉴权 401/200、
  限流 429、SPA 挂载、迁移 v3 回填 48 条标签映射、数据完整性 0 缺失）。
- 前端 `npm run build` 成功；`node scripts/test-markdown.mjs`/`test-clipboard.mjs` 通过。
