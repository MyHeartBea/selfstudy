# FastAPI 核心
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# 数据校验（FastAPI 自带 Pydantic）
pydantic>=2.5.0

# 本地 Windows OCR（截图粘贴后先转文字，再交给 AI 解析）
winsdk>=1.0.0b10

# 图片预处理（本地 OCR 多档增强：灰度/对比度/放大）
Pillow>=10.0.0

# 测试（pytest + TestClient）
pytest>=8.0.0
httpx>=0.27.0
