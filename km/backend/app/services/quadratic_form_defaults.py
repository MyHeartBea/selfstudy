"""考研数学二 线性代数：二次型细分知识点预置内容。"""

QUADRATIC_FORM_NOTES = [
    {
        "tag": "二次型定义与矩阵表示",
        "summary": """## 定义
$n$ 元二次型是关于 $x_1,\\dots,x_n$ 的二次齐次函数：
$$f(x_1,\\dots,x_n)=\\sum_{i=1}^{n}\\sum_{j=1}^{n}a_{ij}x_ix_j$$
约定 $a_{ij}=a_{ji}$，即系数矩阵为对称矩阵 $A=(a_{ij})$，则 $f=x^TAx$。

## 矩阵表示
| 二次型 | 对称矩阵 $A$ |
|---|---|
| $x_1^2+2x_1x_2+x_2^2$ | $\\begin{pmatrix}1&1\\\\1&1\\end{pmatrix}$ |
| $x_1^2-2x_1x_2+3x_2^2$ | $\\begin{pmatrix}1&-1\\\\-1&3\\end{pmatrix}$ |

- 二次型与对称矩阵一一对应；二次型的秩就是 $r(A)$。
- 可逆线性变换 $x=Cy$ 后：$f=y^T(C^TAC)y$。

## 特点
- 交叉项 $x_ix_j$ 的系数在矩阵中分成两半，各为 $a_{ij}/2$。
- 只含平方项时，矩阵为对角矩阵。""",
        "related": "实对称矩阵,二次型标准形与规范形,线性代数",
    },
    {
        "tag": "二次型标准形与规范形",
        "summary": """## 标准形
只含平方项：$d_1y_1^2+\\cdots+d_ny_n^2$，其中 $d_i$ 不全为 0。
通过可逆线性变换即可得到标准形；标准形不唯一。

## 规范形
标准形再归一，系数只取 $1,-1,0$：
$$y_1^2+\\cdots+y_p^2-y_{p+1}^2-\\cdots-y_{p+q}^2$$
其中 $p+q=r(A)$，规范形唯一。

| 名称 | 形式 | 是否唯一 |
|---|---|---|
| 标准形 | $\\sum d_i y_i^2$ | 不唯一 |
| 规范形 | $\\sum y_i^2 - \\sum y_j^2$ | 唯一 |

## 秩
$r(A)=p+q$，即非零平方项个数。""",
        "related": "二次型定义与矩阵表示,惯性定理与正负惯性指数,正交变换化二次型为标准形",
    },
    {
        "tag": "合同矩阵与合同变换",
        "summary": """## 定义
存在可逆矩阵 $C$ 使 $B=C^TAC$，则 $A$ 与 $B$ 合同，$x=Cy$ 称为合同变换。

## 性质
- 合同矩阵秩相同：$r(A)=r(B)$。
- 合同矩阵正、负惯性指数相同。
- 合同 $\\Rightarrow$ 等价；相似不一定合同，合同不一定相似。
- 若 $C$ 是正交矩阵，则 $C^T=C^{-1}$，此时合同同时是相似。

## 判断
判断两个矩阵是否合同：比较秩与正负惯性指数（或特征值正负个数）。""",
        "related": "二次型定义与矩阵表示,惯性定理与正负惯性指数,实对称矩阵",
    },
    {
        "tag": "惯性定理与正负惯性指数",
        "summary": """## 定理
二次型 $x^TAx$ 经任意可逆线性变换化为规范形后，正平方项个数 $p$ 与负平方项个数 $q$ 保持不变。

- $p$：正惯性指数；$q$：负惯性指数。
- $p+q=r(A)$；$p-q$ 称为符号差。

## 应用
- 判断合同：两个矩阵的 $p,q$ 相同则合同。
- 判断正定：$p=n$。
- 正交变换下，$p$ 等于正特征值个数，$q$ 等于负特征值个数。""",
        "related": "合同矩阵与合同变换,正定二次型与正定矩阵,二次型标准形与规范形",
    },
    {
        "tag": "正交变换化二次型为标准形",
        "summary": """## 步骤
1. 写出对称矩阵 $A$；
2. 求 $A$ 的特征值 $\\lambda_1,\\dots,\\lambda_n$；
3. 对每个特征值求线性无关特征向量，同特征值内部正交化并全部单位化；
4. 拼成正交矩阵 $Q$，令 $x=Qy$；
5. 得 $f=\\lambda_1y_1^2+\\cdots+\\lambda_ny_n^2$。

## 特性
- 正交变换保持向量长度与内积，几何意义清晰。
- 标准形系数就是特征值。
- 不同特征值对应特征向量自动正交；重特征值需施密特正交化。""",
        "related": "特征值与特征向量,实对称矩阵,二次型标准形与规范形",
    },
    {
        "tag": "配方法化二次型为标准形",
        "summary": """## 步骤
有平方项：先对含 $x_1$ 的项配方，再逐步消去交叉项。
无平方项：先做可逆线性变换引入平方项，如令 $x_1=y_1+y_2$、$x_2=y_1-y_2$。

## 示例
$f=x_1^2+2x_1x_2+2x_2^2=(x_1+x_2)^2+x_2^2$，令 $y_1=x_1+x_2$、$y_2=x_2$ 得标准形 $y_1^2+y_2^2$。

## 与正交变换的区别
| 方法 | 变换类型 | 标准形系数 |
|---|---|---|
| 配方法 | 可逆线性变换 | 配方得到的任意数 |
| 正交变换 | 正交矩阵 | 特征值 |""",
        "related": "二次型标准形与规范形,正交变换化二次型为标准形",
    },
    {
        "tag": "正定二次型与正定矩阵",
        "summary": """## 定义
对任意 $x\\neq0$，$f=x^TAx>0$，则 $A$ 正定。

## 等价条件
| 条件 | 表述 |
|---|---|
| 定义 | $\\forall x\\neq0$，$x^TAx>0$ |
| 特征值 | 全部为正 |
| 顺序主子式 | 全部大于 0 |
| 合同 | 合同于单位矩阵 $E$ |
| 分解 | 存在可逆 $C$ 使 $A=C^TC$ |
| 惯性指数 | 正惯性指数 $p=n$ |

## 必要条件
- 主对角线元素 $a_{ii}>0$；
- $\\det A>0$。

## 注意
判断正定最常用顺序主子式全正；半正定不能用“顺序主子式全非负”直接判定。""",
        "related": "惯性定理与正负惯性指数,二次型定义与矩阵表示,特征值与特征向量",
    },
    {
        "tag": "半正定负定与不定的判定",
        "summary": """## 判定表
| 类型 | 特征值条件 | 常用判定 |
|---|---|---|
| 正定 | 全部 $>0$ | 顺序主子式全 $>0$ |
| 半正定 | 全部 $\\ge0$ 且至少一个 0 | 特征值非负；或合同于 $\\begin{pmatrix}E_r&0\\\\0&0\\end{pmatrix}$ |
| 负定 | 全部 $<0$ | $-A$ 正定，或奇数阶顺序主子式 $<0$、偶数阶 $>0$ |
| 不定 | 既有正又有负 | 存在正、负特征值 |

## 易错
- “顺序主子式全非负”不能直接推出半正定。
- 半正定可等价于存在矩阵 $C$（不必可逆）使 $A=C^TC$。""",
        "related": "正定二次型与正定矩阵,特征值与特征向量,合同矩阵与合同变换",
    },
    {
        "tag": "二次型综合考法",
        "summary": """## 高频题型
- 由二次型写出对称矩阵，或由矩阵写二次型。
- 化标准形：正交变换、配方法。
- 判断合同：比较秩与惯性指数。
- 判断正定：顺序主子式、特征值、惯性指数。
- 由标准形或规范形反求参数。
- 与特征值、秩、迹、行列式综合：$r(A)=p+q$，$\\mathrm{tr}(A)=\\sum a_{ii}$，$\\det A=\\prod\\lambda_i$。

## 易错点
- 交叉项系数要“一人一半”放进矩阵。
- 配方法中变换必须可逆。
- 规范形唯一、标准形不唯一。
- 合同不要求相似，相似不要求合同；正交相似时两者同时成立。
- 半正定不能用顺序主子式全非负直接判定。""",
        "related": "特征值与特征向量,合同矩阵与合同变换,正定二次型与正定矩阵,二次型标准形与规范形",
    },
]
